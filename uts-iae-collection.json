{
	"info": {
		"_postman_id": "f5f5f5f5-f5f5-f5f5-f5f5-f5f5f5f5f5f5",
		"name": "UTS IAE - Food Delivery Microservices (via API Gateway)",
		"schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json",
		"description": "Koleksi API untuk proyek Microservices Food Delivery (User, Restaurant, Order, Payment) diakses melalui API Gateway (Port 3000).",
		"_uid": "123456-f5f5f5f5f5f5f5f5f5f5f5f5f5f5"
	},
	"item": [
		{
			"name": "1. AUTHENTICATION",
			"description": "Untuk mendapatkan Token JWT dengan role-based access.\n\n**Available Roles:**\n- `admin`: Full CRUD access (GET, POST, PUT, DELETE) to all services\n- `user`: Read-only access (GET only) to all services\n\n**Login Credentials:**\n- Admin: username=`admin`, password=`admin123`\n- User: username=`rizky`, password=`user123`",
			"item": [
				{
					"name": "POST Login (Get JWT Token)",
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "Content-Type",
								"value": "application/json"
							}
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"username\": \"admin\",\n    \"password\": \"admin123\"\n}"
						},
						"url": {
							"raw": "{{base_url}}/auth/login",
							"host": [
								"{{base_url}}"
							],
							"path": [
								"auth",
								"login"
							]
						}
					},
					"response": [],
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"// Simpan token dan user_id ke Collection Variables jika login berhasil",
									"pm.test(\"Status code is 200\", function () {",
									"    pm.response.to.have.status(200);",
									"});",
									"pm.test(\"Login successful\", function () {",
									"    pm.expect(pm.response.json().success).to.be.true;",
									"});",
									"",
									"var jsonData = pm.response.json();",
									"if (jsonData.success) {",
									"    pm.collectionVariables.set(\"token\", jsonData.access_token || jsonData.token);",
									"    pm.collectionVariables.set(\"refresh_token\", jsonData.refresh_token);",
									"    pm.collectionVariables.set(\"user_id\", jsonData.user.id);",
									"    console.log(\"Access Token set: \" + (jsonData.access_token || jsonData.token).substring(0, 15) + \"...\");",
									"    console.log(\"Refresh Token set: \" + jsonData.refresh_token.substring(0, 15) + \"...\");",
									"    console.log(\"User ID set: \" + jsonData.user.id);",
									"    console.log(\"Access Token expires in: \" + jsonData.token_info.access_token_expires_in);",
									"    console.log(\"Refresh Token expires in: \" + jsonData.token_info.refresh_token_expires_in);",
									"}"
								],
								"type": "text/javascript"
							}
						}
					]
				},
				{
					"name": "POST Login as User (Read-Only Access)",
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "Content-Type",
								"value": "application/json"
							}
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"username\": \"rizky\",\n    \"password\": \"user123\"\n}"
						},
						"url": {
							"raw": "{{base_url}}/auth/login",
							"host": [
								"{{base_url}}"
							],
							"path": [
								"auth",
								"login"
							]
						}
					},
					"response": [],
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {",
									"    pm.response.to.have.status(200);",
									"});",
									"var jsonData = pm.response.json();",
									"if (jsonData.success) {",
									"    pm.collectionVariables.set(\"token\", jsonData.access_token || jsonData.token);",
									"    pm.collectionVariables.set(\"refresh_token\", jsonData.refresh_token);",
									"    pm.collectionVariables.set(\"user_id\", jsonData.user.id);",
									"    console.log(\"Access Token set: \" + (jsonData.access_token || jsonData.token).substring(0, 15) + \"...\");",
									"    console.log(\"Refresh Token set: \" + jsonData.refresh_token.substring(0, 15) + \"...\");",
									"    console.log(\"Logged in as User (read-only): \" + jsonData.user.username);",
									"    console.log(\"Role: \" + jsonData.user.role);",
									"}"
								],
								"type": "text/javascript"
							}
						}
					]
				},
				{
					"name": "POST Refresh Token (Get New Access Token)",
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "Authorization",
								"value": "Bearer {{refresh_token}}",
								"description": "Refresh Token"
							},
							{
								"key": "Content-Type",
								"value": "application/json"
							}
						],
						"url": {
							"raw": "{{base_url}}/auth/refresh",
							"host": [
								"{{base_url}}"
							],
							"path": [
								"auth",
								"refresh"
							]
						}
					},
					"response": [],
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {",
									"    pm.response.to.have.status(200);",
									"});",
									"var jsonData = pm.response.json();",
									"if (jsonData.success) {",
									"    pm.collectionVariables.set(\"token\", jsonData.access_token || jsonData.token);",
									"    console.log(\"New Access Token set: \" + (jsonData.access_token || jsonData.token).substring(0, 15) + \"...\");",
									"    console.log(\"Access Token expires in: \" + jsonData.token_info.access_token_expires_in);",
									"}"
								],
								"type": "text/javascript"
							}
						}
					]
				}
			]
		},
		{
			"name": "2. USER SERVICE (Protected)",
			"description": "Endpoint Service Pengguna (User Service) via API Gateway.\n\n**Role Requirements:**\n- GET requests: `admin` and `user` ✅\n- POST, PUT, DELETE: `admin` only 🔒",
			"item": [
				{
					"name": "GET All Users",
					"request": {
						"method": "GET",
						"header": [
							{
								"key": "Authorization",
								"value": "Bearer {{token}}"
							}
						],
						"url": {
							"raw": "{{base_url}}/api/user/users/",
							"host": [
								"{{base_url}}"
							],
							"path": [
								"api",
								"user",
								"users",
								""
							]
						}
					},
					"response": []
				}
			]
		},
		{
			"name": "3. RESTAURANT SERVICE (Protected)",
			"description": "Endpoint Service Restoran (Restaurant Service) via API Gateway.\n\n**Role Requirements:**\n- GET requests: `admin` and `user` ✅\n- POST, PUT, DELETE: `admin` only 🔒",
			"item": [
				{
					"name": "GET All Restaurants",
					"request": {
						"method": "GET",
						"header": [
							{
								"key": "Authorization",
								"value": "Bearer {{token}}"
							}
						],
						"url": {
							"raw": "{{base_url}}/api/restaurant/restaurants/",
							"host": [
								"{{base_url}}"
							],
							"path": [
								"api",
								"restaurant",
								"restaurants",
								""
							]
						}
					},
					"response": []
				},
				{
					"name": "GET Menu by Restaurant ID 1",
					"request": {
						"method": "GET",
						"header": [
							{
								"key": "Authorization",
								"value": "Bearer {{token}}"
							}
						],
						"url": {
							"raw": "{{base_url}}/api/restaurant/restaurants/{{restaurant_id}}/menu",
							"host": [
								"{{base_url}}"
							],
							"path": [
								"api",
								"restaurant",
								"restaurants",
								"{{restaurant_id}}",
								"menu"
							]
						}
					},
					"response": []
				}
			]
		},
		{
			"name": "4. ORDER & PAYMENT SERVICE (Protected)",
			"description": "Endpoint Service Pesanan (Order Service) yang akan memicu proses pembayaran.\n\n**Role Requirements:**\n- GET requests: `admin` and `user` ✅\n- POST orders: `admin` only 🔒",
			"item": [
				{
					"name": "POST Create Order (Full Flow)",
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "Content-Type",
								"value": "application/json"
							},
							{
								"key": "Authorization",
								"value": "Bearer {{token}}"
							}
						],
						"body": {
							"mode": "raw",
							"raw": "{\n    \"user_id\": {{user_id}}, \n    \"restaurant_id\": {{restaurant_id}}, \n    \"items\": [\n        {\n            \"menu_item_id\": {{menu_item_id}}, \n            \"quantity\": 2\n        }\n    ]\n}"
						},
						"url": {
							"raw": "{{base_url}}/api/order/orders/",
							"host": [
								"{{base_url}}"
							],
							"path": [
								"api",
								"order",
								"orders",
								""
							]
						}
					},
					"response": []
				},
				{
					"name": "GET All Orders",
					"request": {
						"method": "GET",
						"header": [
							{
								"key": "Authorization",
								"value": "Bearer {{token}}"
							}
						],
						"url": {
							"raw": "{{base_url}}/api/order/orders/",
							"host": [
								"{{base_url}}"
							],
							"path": [
								"api",
								"order",
								"orders",
								""
							]
						}
					},
					"response": []
				}
			]
		},
		{
			"name": "5. PAYMENT SERVICE (Protected)",
			"description": "Endpoint Service Pembayaran (Payment Service) via API Gateway.\n\n**Role Requirements:**\n- GET requests: `admin` and `user` ✅\n- PUT, DELETE: `admin` only 🔒",
			"item": [
				{
					"name": "GET All Transactions",
					"request": {
						"method": "GET",
						"header": [
							{
								"key": "Authorization",
								"value": "Bearer {{token}}"
							}
						],
						"url": {
							"raw": "{{base_url}}/api/payment/payments/",
							"host": [
								"{{base_url}}"
							],
							"path": [
								"api",
								"payment",
								"payments",
								""
							]
						}
					},
					"response": []
				}
			]
		}
	],
	"variable": [
		{
			"key": "base_url",
			"value": "http://localhost:3000",
			"type": "string",
			"description": "URL dasar API Gateway Anda."
		},
		{
			"key": "token",
			"value": "PASTE_TOKEN_HERE_OR_LOGIN_AUTOMATICALLY",
			"type": "string",
			"description": "Access Token JWT yang didapatkan setelah login (expires in 1 hour). Diisi otomatis oleh Test Script Login."
		},
		{
			"key": "refresh_token",
			"value": "",
			"type": "string",
			"description": "Refresh Token JWT untuk mendapatkan access token baru (expires in 7 days). Diisi otomatis oleh Test Script Login."
		},
		{
			"key": "user_id",
			"value": "1",
			"type": "string",
			"description": "ID Pengguna untuk testing. Diisi otomatis oleh Test Script Login."
		},
		{
			"key": "restaurant_id",
			"value": "1",
			"type": "string",
			"description": "ID Restaurant default (dari data inisialisasi)."
		},
		{
			"key": "menu_item_id",
			"value": "1",
			"type": "string",
			"description": "ID Menu Item default (dari data inisialisasi)."
		}
	]
}